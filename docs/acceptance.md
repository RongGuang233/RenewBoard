# 1.6.0 验收

- 首页总额进入按日期分组的未来30天扣款页，首页笔数、明细与汇总共用扣款事件；保留原始周期锚点、已记录的外币折算依据和话费分期生效月费，零元周期不计扣款笔数。支持进入订阅后逐层返回。
- 退款支持本机草稿保留、放弃、重开和Activity重建恢复；空白或改回默认值直接退出。保存等待数据库提交，失败保留输入，成功才清除草稿。草稿不加入账本备份。
- 原付款用一行关联退款入口，退款可查看原付款；关联列表排除其他付款的退款，逐层返回保留原搜索及列表位置。
- 话费详情精简余额卡、最近三笔可点击，“全部记录”进入独立历史页；按账户ID筛选，同名账户不会混合，充值仍单列且不计支出。账本明确净支出，付款/退款构成默认收起，月费记录附简短来源。
- 设备列表按状态显示购入金额、已卖出净花费或预算，保持原有设备统计口径及五个主导航。
- 113项JVM测试通过；40项相关设备场景通过（分组运行及定向复测）：新页面/金额3、退款草稿3、账本11、话费6、设备3、Room/备份8、付款4、订阅草稿2。首轮新测试遗漏必需权益样例，已补齐；账本测试结束时发生一次Compose销毁异常，独立复测通过，补齐清理后等待界面稳定，账本整组及后续34项均通过。
- debug/release构建及lint通过，均0 errors、12 warnings；未新增依赖、数据库字段或备份格式。检查了未来扣款、话费精简详情/历史、净支出构成、退款关联页面的模拟器截图。
- versionCode=13，versionName=1.6.0，沿用原发布证书。API35模拟器从1.5.0覆盖安装并启动成功，升级前后BookRow完整JSON一致（1个订阅、2项权益、1笔付款）。界面演示仅使用独立debug包的虚构数据，检查后已核对并恢复原空测试账本、关闭模拟器；未操作实体手机或真实坚果云数据。
- APK `RenewBoard-1.6.0-release.apk`：14,744,601 bytes；SHA256 `a66cde1810f67ad5c1bbcd79b8071a57eecb6549ca4ce98cab21df1e89ee81ab`。

# 1.5.0 验收

- 首页压缩标题、汇总卡和到期行；需要充值的话费优先显示，余额充足的账户放在到期事项之后。人民币汇总保留蓝色视觉重点。
- 选中趋势日期、月份或年份后，明细入口跟随时段；应用排行进入该应用的时段明细，逐层返回保留筛选。趋势点击仍只更新排行。
- 多期续费显示实付总额与实际周/月/年时长；未手动修改时按期数合计，手动优惠价及旧付款草稿保留。话费指定生效月份改用独立年份入口和月份网格，取消不写回。
- 设备分别记录停止服役日与卖出日，默认关联，仅需要时展开额外字段；退役后卖出保留原停止日期。旧已卖出记录以原 endDate 兼作卖出日期，不改写旧记录；服役天数按停止日期冻结。
- 退款为关联原付款的独立记录，冲减退款日期所在时段支出，不推进权益、不改变话费余额。允许多次部分退款，原币累计不超过原付款；外币实际人民币到账独立冻结，允许因结算差异高于原人民币实付。删除原付款一并删除关联退款，确认前显示数量；趋势和排行支持负净支出。
- 延后提醒绑定实际事件；部分联合权益续费后保留未续项，全部续费、余额充值或校准解决后清理旧通知和任务。错过的近7天提醒合并补发一次；关闭权限不消费提醒记录，随后开启可补发。
- 105项JVM测试通过。58项相关设备场景通过（分组运行及定向复测）：付款4、话费6、设备草稿3、设备3、账本9、Room/备份8、主界面17、订阅草稿2、设置3、提醒3。首轮修正测试中的重建同步、屏外坐标、菜单硬件返回及误填的设置类名；保留金额、日期和持久化断言。提醒测试发现部分续费生成的新事件会阻止旧任务取消，已改用 WorkManager 原绑定事件，最终4项退款/通知场景复测通过。
- debug/release构建及lint通过，均0 errors、12 warnings；未新增依赖。已检查首页、付款、退款负值趋势、设备和月份选择器截图。新退款和设备日期实际Room重开、JSON备份恢复通过，旧数据默认值兼容；含新字段的备份请在1.5.0及以后恢复。
- versionCode=12，versionName=1.5.0，沿用原发布证书。API35模拟器从1.4.0覆盖安装并启动成功，升级前后BookRow完整JSON一致（1个订阅、2项权益、1笔付款）。演示检查只在独立debug包进行，原空测试账本已恢复；未操作实体手机或真实坚果云数据。
- APK `RenewBoard-1.5.0-release.apk`：14,711,833 bytes；SHA256 `8faf08ef65e98f680aacd0aeea353605a593812f91e55353e3a346772167c977`。

# 1.4.0 验收

- 新增先进入会员选择页，支持搜索、分类、最近使用和自定义；金额与币种并排，联合权益及备注默认收起。订阅、设备和付款表单固定底部保存；付款改为独立页面，统一显眼的返回入口，修正系统栏与键盘重复留白。
- 三类表单支持本机完整草稿：保留、放弃、重开和页面重建恢复，成功保存后清除；撤回全部输入到原值时清除旧草稿，不产生多余提示。订阅草稿保留周期倍数、外币实付、联合权益手动日期和跟随关系；付款草稿按订阅隔离；设备草稿保留暂时隐藏的卖出金额与日期。
- 一次续费支持1–120期，只生成一笔实际付款；保留原周期、过期重开、联合权益选择及外币实付冻结。相同订阅、日期、币种和数值金额时提醒相似付款，允许返回检查或明确继续，不禁止真实同日同额付款。
- 话费月费可立即、下月或指定月份生效。先按旧规则结算，再按生效日切换金额；余额、未来扣款及月费支出使用对应价格。可替换或取消待生效调整；充值、校准和归档保留调整，生效日在扣费日之间也不回算旧月费。
- 88项JVM测试通过。48项相关设备场景通过：订阅草稿2、主界面16、付款3、设备草稿2、话费5、设备3、Room/备份7、账本7、设置3。首轮旧文案匹配及键盘动画中的付款点击未适配，改为匹配实际精简文案和固定按钮语义点击；47项回归通过后，最后6项草稿及错误提示定向复测全部通过，保留金额、日期、权益和持久性断言。
- debug/release构建通过，lint均0 errors（12 warnings）。实际检查默认收起的订阅、付款和会员选择截图。未新增依赖；BalanceAccount增加可选pendingFee字段，旧数据及旧备份读取、待生效调整JSON往返测试通过，新备份使用1.4.0及以后版本恢复。草稿仅存本机，不加入统计或账本备份。
- versionCode=11，versionName=1.4.0，沿用原发布证书。API35模拟器由1.3.0覆盖安装并启动成功，升级前后BookRow完整JSON相同（1个订阅、2项权益、1笔付款）。未操作实体手机或真实坚果云数据。
- APK `RenewBoard-1.4.0-release.apk`：14,679,065 bytes；SHA256 `1e6d67092805aaf93f205ad33fcfad0aa4b722925234246da13be757f6a6d1e3`。

# 1.3.0 验收

- 付款支持仅记账及更正金额、日期、备注；外币仍要求人民币实付，修改不推进权益或扣款周期。过期续费明确选择从付款日重开或补交原周期，预览到期日和下次扣款；取消过期权益选择会重算处理方式，保留其他联合权益。
- 历史充值可明确选择是否已包含在已知余额中；校准余额可将减少的差额记为话费额外扣费。充值不重复计入支出，话费月费及额外扣费归入支出。
- 订阅搜索、自动续费筛选与排序，首页过期项目收起；外币缺少依据时显示其他已知人民币预计总额和待补录项目。设备搜索、排序、卖出净花费及日均购入成本口径；表单数字键盘、币种选择、折叠备注与统一返回，低频操作移入菜单。
- 通知直接定位订阅，可确认扣款或明天提醒；关闭自动续费仍保留到期提醒。设置快捷与自定义提醒天数，刷新系统通知授权及备份结果。提醒受系统后台调度影响，不承诺精确时间。
- 修复通知切换沿用上一订阅草稿、重建时通知重复处理，以及 Scaffold 内容混用实时加载状态和旧账本快照导致丢失详情的问题。实际界面验证A订阅未保存付款草稿不会带入B订阅，完成付款并重建后仍留在B详情且不重复打开付款。
- 76项JVM测试通过；46项设备场景通过（按受影响分组运行与复测：日期4、设备3、话费4、设置3、账本7、主界面16、数据库7、通知2）。菜单及搜索测试定位、页面重建等待先后修正；保留付款金额、权益及数据持久性断言，未以重试掩盖实际失败。
- 日期增加逐月切换和今天入口。测试识别出跨Dialog的模拟字体缩放无效，改用真实系统font_scale=1.6独立验证字形边界、无截断和可选日期；结束恢复原设置。普通字体三项日期回归也通过，实际大字体截图与主要页面截图已检查。
- debug/release构建、lint和测试脚本语法检查通过；未新增依赖或改变业务存储与备份格式。versionCode=10、versionName=1.3.0，沿用原发布证书。API35模拟器由1.2.2覆盖安装并启动成功，升级前后BookRow完整JSON相同（1个订阅、2项权益、1笔付款）。未操作实体手机或真实坚果云数据。
- APK `RenewBoard-1.3.0-release.apk`：14,613,529 bytes；SHA256 `e6c63d24d2d8cb62333ab99b33237ad3bab7933a9676b13abfee57dcce6170e1`。

# 1.2.2 验收

- 共享日期选择器改为独立且常显的年份、月份按钮；年份滚动选择，月份12格直接点选，日期按当月天数展示，确认才写回。保留文本输入。验证闰年2月29日、平年2月28日、从31日切短月份及取消不修改日期。
- 设备分类扩充至16类，采用可搜索、限高滚动的三列图标网格；保留旧分类序列化名称。验证手机及新增电纸书分类选择、搜索无结果、保存/重开，以及新旧分类备份往返。
- 移除设备详情的日均公式解释，保留日均金额。实际蓝色界面截图已检查。
- 64项JVM测试、18项相关设备测试通过（日期2、设备2、话费3、主界面11）。表单回归的指针点击在输入法滚动期间未触发保存，改用按钮语义点击后14项表单回归全部通过；新增日期及分类面板仍通过实际点击验证。
- debug/release构建和lint通过，沿用原发布证书。versionCode=9、versionName=1.2.2；API35模拟器从1.2.1覆盖安装并启动成功，原BookRow完整JSON相同（1个订阅、2项权益、1笔付款）。未连接实体手机或真实坚果云账号。
- APK `RenewBoard-1.2.2-release.apk`：14,547,989 bytes；SHA256 `74e5dff0504c15090190b13e44ed2b517ad671bb3c80d930894f7c541b971baa`。无新依赖或备份格式变更。

# 1.2.1 验收

- “更新与关于”显示订阅簿、作者 RongGuang233 和动态版本号。
- 用户确认话费采用月费支出口径：余额账户计入订阅与未来30天预计扣款；打开应用时补记余额记录日期之后、截至当天的设定月费。月费来自用户设置而非运营商账单，历史充值保留但视为余额流转、不计支出。
- 月费与余额日期在同一 Room 事务推进，重复打开/并发结算不重复记账，删除扣费记录后不重新生成。修改月费不改历史金额，归档期间暂停记账，恢复不倒补归档月份。已知余额日期以前的费用不倒推；余额校准日期不能早于最近已记录余额日期。
- 每月扣费只填1–31日，不再要求年份；无对应日期按月末。余额字段明确为“查询到的余额 / 余额查询日期”，默认今天。
- 设备列表默认服役中，其他状态放入下拉筛选；列表、详情和编辑页紧凑化。已卖出显示卖出日期及金额，已退役显示退役日期；沿用原数据字段。
- 63项JVM测试通过；32项相关设备测试通过（Balance3、Devices2、Settings1、Ledger6、Repository7、MainActivity11、通知2）。首轮横向筛选测试点击了未滚入视野的“全部”，修正滚动后该项通过。额外验证月费扣除导致余额不足时仍按当天提醒、通知不会重复。
- debug/release构建、lint与原证书签名验证通过。versionCode=8，versionName=1.2.1；API35模拟器从1.2.0覆盖安装成功，旧演示账本 JSON 完全保留（1个订阅、2项权益、1笔付款）。新增月费通过独立测试验证。未连接真实坚果云或实体手机。
- APK `RenewBoard-1.2.1-release.apk`：14,515,225 bytes；SHA256 `6b5ebd29af5f179ccde25eba0b310bec1c7437e7cc7c98e8165ae7350722908a`。未新增依赖或更改备份格式。

# 1.2.0 验收

- 账本移除蓝色实付总览卡。趋势柱仅选中对应日/月/年并更新应用排行，不打开明细；再次点击取消选中，切换范围重置。排行显示明确时段。
- 全部明细、单笔详情和外币补录使用独立页面，逐层系统返回。明细按日期分组，每行不再重复日期及“订阅付款”标签；保留原币金额和人民币冻结金额、搜索、分类与删除。
- 设置首页只保留到期提醒、本地备份、坚果云 WebDAV、更新与关于；表单、云端列表和许可证为子页。后台下载沿用原实现。
- 新增设备导航及服役中、已退役、已卖出、待购买四状态，支持创建、编辑、删除、分类、购价/预算、起止日期、卖价和备注。服役天数包含首日；日均按购价除以服役天数，退役/卖出冻结结束日期，待购买不算服役。设备金额不加入订阅付款统计。
- Ledger 新增默认空 devices，不改变 Room 表结构。实际数据库验证旧无 devices JSON 可读取，添加设备后保留原订阅/权益/付款，数据库重开和备份恢复保留设备。旧备份恢复会清空设备；恢复确认明确提示整体替换。含设备的新备份应使用1.2.0及以后版本。
- 60项JVM测试通过：Domain24、Prepaid5、LedgerStats9、Devices6、WebDav11、Updates5。25项相关设备测试通过：MainActivity11、Ledger5、Repository6、Settings1、Devices2。首轮两个测试操作问题（异步切页后立即返回、整页保存按钮尚未滚入键盘上方）修正后复测通过，未放宽业务断言。最终截图已经逐张检查。
- debug/release构建和lint通过；未新增依赖。versionCode=7，versionName=1.2.0，使用原发布证书。API35模拟器从1.1.4覆盖安装、启动成功，升级前后BookRow完整JSON相同（1个订阅、2项权益、1笔付款）。未修改实体手机或真实坚果云数据。
- APK `RenewBoard-1.2.0-release.apk`：14,515,221 bytes；SHA256 `9b7861b06389ffcf99101ebf2a57ab014461a348b12428277e0e9f69fc794407`。

# 1.1.4 验收

- 正式版包含权益日期联动与手动覆盖、话费余额/充值/校准/余额提醒，以及统计概览账本和横向应用支出柱状图。
- 趋势支持近30天、近3/6/12月、自然年、近5年和全部；自然年本年统计截至当天。实付仅使用付款日冻结人民币金额，缺失金额单独提示。付款列表可搜索、分类及筛选已删订阅，支持单笔/批量删除；删除订阅可选择是否同时删除付款。
- 54项单元测试、14项账本及原有界面测试通过；横向柱状图最终调整另通过1项已有界面场景，截图已检查。此前余额与通知专项验收证据仍有效，未重复整轮运行。
- 正式构建及lint通过。APK沿用原签名，versionCode=6，versionName=1.1.4；API35模拟器覆盖安装及启动成功，前后BookRow JSON完全一致（1个订阅、2项权益、1笔付款）。未修改真实手机或坚果云数据。
- APK：`RenewBoard-1.1.4-release.apk`，14,466,069 bytes；SHA256：`e37f9638b71bb810a010bc3628fce80021de30c2a31ba582545df1f027f86869`。
- 1.1.3为此前本地候选，只有成品与验收记录，未建立独立源码发布提交。其原包作为历史候选附件保留，不建立混淆源码身份的v1.1.3正式标签。原包14,433,301 bytes，SHA256：`34ba0c6bd29f42b50357e02aaf0b058bcd6736a8fa4b6f4e22ce6df206a462e7`；正式更新入口仅使用1.1.4。
- 1.1.4读取旧备份；包含余额账户的新备份请用1.1.4及以后版本恢复。付款删除不会撤销权益或话费余额，需校准时使用余额编辑。

# 1.1.2 验收

- 更新入口改为应用内下载，显示进度、支持取消；下载由Android DownloadManager管理，重新进入页面可恢复状态，完成后唤起系统安装界面。
- 首次安装需用户允许此来源安装应用，最终由系统确认；下载来源仍为本项目GitHub正式发布附件。
- 40项JVM测试通过；2项设备验收分别验证取消/记录丢失恢复，以及公开APK真实下载、文件一致性、状态恢复和打开系统更新确认界面。真实下载验收需模拟器临时使用本机代理，测试后已撤销代理与安装权限；未点击确认安装。
- release构建、lint通过；最终1.1.2正式包在API35模拟器覆盖安装及启动成功，versionCode=4，沿用原证书。未连接实体手机。
- APK：`RenewBoard-1.1.2-release.apk`，14,416,917 bytes；SHA256：`a0db1334b858b7de96eee24031f05337c349dbd51d34f0f07e1f6e4bcfe09acf`。
- 1.1.1及更早版本这次仍需手动下载安装包，安装1.1.2后未来更新可在应用内完成下载。业务数据格式未变更。

# 1.1.1 验收

- 新增和详情页面支持系统返回；编辑返回详情；弹窗优先关闭。顶部返回按钮固定显示，带箭头与蓝色底色。
- 周期改为数字输入及周/月/年下拉选择；扣款日期、到期日期标签简化，保留日历选择；权益默认收起。
- 9项相关界面测试通过（8项原有流程及1项返回导航回归），覆盖系统返回、页面按钮、编辑页重建、弹窗关闭、周期保存及权益折叠。
- release构建及lint通过；API35模拟器覆盖安装和启动成功。versionCode=3，versionName=1.1.1，沿用原发布证书。
- APK：`RenewBoard-1.1.1-release.apk`，14,416,889 bytes；SHA256：`6f1b46385c775c666c9812257c3ba5d2acbc6496d0dacecdfeba3b198a9f7b35`。
- 本次未连接实体手机；业务存储格式和备份流程未变更。

# 1.1.0 验收

2026-09-06：蓝色界面、人民币汇总、62项会员目录与官方应用图标、外币付款冻结、检查更新均已集成到Android应用。

- 39项JVM测试通过：领域24、WebDAV11、更新检查4。
- 14项设备测试通过：Room5、Compose7、通知2。新增分类测试最初同时匹配筛选按钮和标题，修正定位后重跑该项及授权通知测试通过，其余12项首次通过。
- 首次外币付款与续费分别冻结人民币实付金额；修改手动汇率不改变历史总额。旧外币付款可补录，JSON恢复及Activity重建保留冻结金额。
- 首页仅显示人民币预计总额：按同套餐同币种最近有效付款的实际折算比例计算；无依据时提示补录，不按当前汇率回算历史。
- 62项预设、7类筛选、59张离线官方图标已集成；小黑盒加速器筛选、选择、填写与保存流程通过。
- 检查更新只请求GitHub公开正式发布信息，支持新版本、无更新及错误状态；通过版本比较和HTTP错误测试，静态预览实际查询成功。下载在浏览器进行，由用户完成安装。
- 蓝色首页、会员目录、订阅列表、付款账本截图已检查；debug、androidTest、release构建通过，lint 0 errors。
- 最终APK覆盖安装成功，versionCode=2，versionName=1.1.0，minSdk=26，targetSdk=35。v2签名验证通过，与1.0.0使用同一证书。
- APK大小 14,400,505 bytes；SHA-256 `4fa6cf666e83fc501df128544a06bf23fada2ebfcd546b456076f9569bbe9bb9`。

本轮合计 **53项自动测试通过**。设备为API35 ARM64模拟器。真实坚果云账号与实体手机长期后台仍未验证。1.1.0可读取1.0.0备份；新备份请使用1.1.0及以后版本恢复。

以下保留1.0.0历史验收记录。

---

# 1.0.0 本地验收

2026-09-06，已完成本地源码、签名 release 构建与 API35 ARM64 模拟器验收。未连接真机、未配置真实坚果云。

## 实际验证结果

| 范围 | 结果 |
| --- | --- |
| JVM 业务测试 | 14/14，通过：周/月/年整数周期、月末/闰年锚点、提前续费、赠送、不同权益期限、套餐只记一次成本、分币种、归档删除保留付款、完整/缺字段/损坏/未知版本 JSON |
| WebDAV 模拟 HTTP 测试 | 11/11，通过：非空完整业务 JSON 的 PUT→GET核验→MOVE，认证头、目录列表、有效恢复内容、上传/核验/MOVE/下载失败、只保留最近10份成功自动备份、手动不删、过期临时文件回收 |
| 真实 Room 设备测试 | 5/5，通过：空库、文件数据库重开持久化、编辑归档删除、坏恢复不改变本机、有效整体恢复、并发提醒 claim 与重开去重 |
| 真实 Compose 设备测试 | 3/3，通过：新增编辑、Activity重建、归档删除后账本仍在、无效输入纠正、Jan31→Feb29→提前续费Mar31→赠送7天Apr7且付款数不变 |
| 系统通知设备测试 | 2/2，通过：拒绝授权不消耗事件；授权后实际通知发出，重复 Worker 执行不刷新通知 postTime，事件已占用 |
| APK 构建 | debug、androidTest、release 均成功；release使用独立可升级签名，非debug密钥 |
| 静态检查 | lintDebug通过，0错误；17项提示为依赖有新版12项、KTX写法4项、旧Android备份配置提示1项。旧Android已设置allowBackup=false，Android12+另明确排除云备份及设备迁移 |
| 最终 release 模拟器检查 | 正式包安装成功；首装空态、快捷录入联合套餐、两项不同到期权益、一笔68 CNY付款、强制停止再打开持久化与账本均已检查；实际系统文件导出、读取预览、确认恢复回环通过 |

合计 **35 项自动测试通过**。设备：Android 15 / API35，ARM64，Pixel 6 AVD，Emulator 37.1.11，独立5554端口。SwiftShader Vulkan路径曾出现黑屏，改用宿主GPU并禁用Vulkan后实际画面正常；这不是Android真机验收。

修复并复测了：API26主题属性版本边界、FAB可访问名称、键盘遮住编辑保存按钮、残缺JSON被当默认空数据、失败远端副本不应挤掉成功备份。

## release 产物

- 文件：`RenewBoard-1.0.0-release.apk`，12,217,514 bytes。
- 包名 `cn.renewboard`，versionName `1.0.0` / versionCode `1`，minSdk26 / targetSdk35。
- SHA-256：`1ff7174bd297e27218d362dff8cfa1802044540398a18f728e78e6f8208b66db`。
- apksigner：Verifies，APK Signature Scheme v2，通过；RSA3072，单签名者。
- 签名证书 SHA-256：`9581b8f6993e5c88f3e5d49c837c1b28c7fa23caae6e2c9ca81e04660ec357aa`。
- 密钥及密码保存在仓库外受限本地目录，不进入源码、APK资源或发布附件。

本地原始验证输出在 `app/build/test-results/testDebugUnitTest/`、`app/build/reports/lint-results-debug.html` 和 `artifacts/`；这些包含本机运行细节，不公开上传。图中订阅均为模拟器演示数据，安装APK默认无数据。

## 已知验证边界

- 真实坚果云未配置，因此没有真实云端上传/下载回环；模拟WebDAV成功不等于真实坚果云兼容性已验收。
- 无连接真机；Android8–14各系统及OEM后台省电行为未逐一测试。minSdk26与API检查通过不替代这些设备测试。
- WorkManager系统延迟、长时间锁屏和弱网下的长期稳定性未作持续数日验证，不承诺准点通知。
- Kotlin/AGP组合超出Kotlin文档的完全支持矩阵上限；本项目实际编译与上述测试已通过，详见许可证说明。

## 公开发布范围

目标为 GitHub `RongGuang233/RenewBoard`，release tag `v1.0.0`。

公开：原创源码、Gradle wrapper与正式依赖清单、测试代码、构建脚本、README、MIT/第三方许可证、隐私及本验收说明、演示截图；release附件只含已核验APK和SHA256校验文件。

排除：签名私钥/密码、本机环境说明、local.properties、Gradle缓存、构建中间文件、测试运行日志、任何其他项目或用户业务数据。
